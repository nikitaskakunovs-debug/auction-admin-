"use client";

import { SupplierPortal } from "@/components/SupplierPortal";

/** Сам кабинет — пять экранов; доступ проверяется внутри компонента. */
export default function SupplierPortalPage() {
  return <SupplierPortal />;
}
